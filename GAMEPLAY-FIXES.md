# 게임플레이 수정 기록

## 2026-09-15 (6차) — 저택을 두 배로, 그리고 정전

요청: 맵이 더 컸으면 좋겠다 / 탐색하는 맛 / 정전처럼 만들고 손전등 / 실제 녹음
파일(피격·문)을 넣어 달라.

### 방을 늘리는 것만으로는 탐색이 되지 않는다

예전 도면은 가운데 대홀이 z 로 뻥 뚫려 있었다. 거기 한 번 서면 좌우 여섯 방의
문이 **한눈에 다 보였다.** 방을 더 붙여도 이 구조가 그대로면 "문이 많은 큰 방"
이지 탐색이 아니다.

그래서 **복도를 두 줄** 넣어 대홀을 가두고, 방을 전부 복도에서 열리게 했다.
다음 방을 보려면 복도를 걸어야 하고, 복도에서는 좌우가 다 열려 있다.

| | 전 | 후 |
|---|---|---|
| 저택 내부 | 48 x 36 m | **70 x 52 m** (2.1배) |
| 구역 전체 | 64 x 68 m | **88 x 88 m** |
| 방 | 7칸 | **17칸 + 복도 2줄** |
| 문 | 15짝 | **36짝** (+ 아치 8곳) |
| 경비 자리 | 28곳 | **54곳** |
| 기본 인원 | 6명 | **8명** (최대 14명) |

```
   x  -35    -23    -11     11     23     35
 z -26 ┌──────┬──────┬──────┬──────┬──────┐
       │ 저장 │ 서재 │ 계단홀│ 온실 │ 작업 │
 z -15 ├──────┴──────┴──────┴──────┴──────┤
       │ ══════════ 북 복도 ════════════ │
 z -11 ├──────┬──────┬──────┬──────┬──────┤
       │ 주방 │ 서고 │      │ 화랑 │ 예배 │
 z   0 ├──────┼──────┤ 대홀 ├──────┼──────┤
       │ 식품 │ 응접 │      │ 객실 │ 하인 │
 z  11 ├──────┴──────┴──────┴──────┴──────┤
       │ ══════════ 남 복도 ════════════ │
 z  15 ├─────────────┬──────┬────────────┤
       │   연회실    │ 현관 │  무도회장  │
 z  26 └─────────────┴──────┴────────────┘
```

방이 17칸인데 인원은 8명이라 **절반 넘게 빈다.** 그게 노린 것이다 — 어느 방이
비었는지 모르니 전부 확인해야 한다. 증거 후보도 6곳에서 9곳으로 늘려 매 판
다른 방을 뒤지게 했다.

계단은 계단홀 **동쪽 벽에 붙였다.** 방 한가운데 두면 홀을 가로지를 수가 없어서
길찾기가 통째로 막힌다(실제로 처음에 그렇게 만들어 놓고 계단홀에 닿지 못했다).

### 조명: 등이 25개인데 광원은 8개까지만 켤 수 있다

`q.pointLights` 는 기기가 감당할 광원 수라 3~8개다. 예전 코드는 목록 **앞에서부터**
그만큼만 켰다. 방이 7칸일 때는 그럭저럭 맞았지만 17칸이 되자 저택 절반이 늘
캄캄했다. 광원을 "자리"가 아니라 **묶음**으로 두고, 0.2초마다 카메라에서 가까운
등에 다시 붙인다. 전구 알맹이는 전부 만들어 두므로 멀리서도 불빛 점은 보인다.

### 정전 (2단계 진입 시점)

저택 안으로 들어서면 저쪽에서 두꺼비집을 내린다.

- 실내등만 전부 꺼진다. 야외등은 담장 밖 배선이라 남는다 — 그래서 창밖은
  어슴푸레하고 안은 캄캄하다.
- 서버의 발견 판정(`brightnessAt`)에서도 실내등이 빠진다. **적도 나를 늦게 보고,
  나도 적을 늦게 본다.**
- **L** 로 손전등. 앞은 보이지만 어둠 속의 불빛은 그 자체가 표적이라 훨씬 먼저
  발견된다(`visibilityFactor` 의 `target.light`). 밝은 곳에서는 손해가 거의 없다.
- 터치 화면에는 '손전등' 버튼이 생긴다.

화면을 어둡게 만들 때 **환경광(`scene.environmentIntensity`)을 같이 내려야 한다.**
등을 아무리 꺼도 화면이 그대로 밝길래 한참 찾았는데, visuals.js 가 깔아 두는
RoomEnvironment 가 실제로는 가장 센 광원이었다.

### 녹음 파일 두 개

합성으로 흉내 내기 가장 어려운 두 소리 — 사람이 맞는 소리(성대)와 문이 닫히는
소리(나무통 울림) — 만 실제 녹음을 쓴다.

| 파일 | 쓰는 곳 |
|---|---|
| `hurt-male.mp3` | 피격(짧고 높게) · 큰 피격 · 쓰러짐(낮고 느리게) |
| `door-slam.mp3` | 강제 개방(느리게 = 무겁게) · 닫기 · 열기(약하고 짧게) |

같은 녹음을 재생 속도와 길이로 나눠 쓴다. 파일 앞뒤의 무음과 긴 잔향 꼬리는
잘라 낸다(브라우저에서 디코딩해 50ms 단위로 재 봤다: hurt 0.89초 중 앞 0.1초가
무음, door 3.03초 중 앞 0.22초가 무음이고 잔향이 2초 넘게 남는다).

**파일을 못 받아 와도 소리는 난다.** 받아오는 동안, 또는 영영 못 받으면 예전
합성음이 그대로 난다. 잘려 올라가는 사고를 막으려고 해시 검사(asset-integrity)
목록에도 넣었다.

## 2026-09-15 (5차) — 대사가 우물거리던 문제와 '인질' 이름표

플레이 중 보고: 목소리가 아직도 이상하다(우물우물거린다) / 대사는 한국어 말고
영어로 해 달라 / 적 머리 위에 '인질' 이라고 붙어 있다.

### 대사는 한국어 목소리가 영어를 읽고 있었다

대사 문구 자체는 처음부터 영어였다(`Police! Drop the weapon!`). 문제는 그것을
읽는 목소리였다.

```js
utterance.lang = 'en-US';   // 이것만으로는 목소리가 정해지지 않는다
```

`lang` 은 "이 언어로 읽어 달라" 는 희망 사항일 뿐이고, `voice` 를 지정하지 않으면
브라우저는 **기본 목소리**로 읽는다. 한국어 환경의 브라우저에서 그 기본값은
한국어 목소리라서, 영어 문장을 한글 발음 규칙으로 뭉개 읽는다. 그게 그 웅얼거림이다.

- `speechSynthesis.getVoices()` 에서 **영어 목소리를 직접 골라** 지정한다.
  미국 영어를 먼저 보고, 그중 또렷하게 읽는 쪽(Google/Neural/Natural 계열)을 올린다.
- 영어 목소리가 **아예 없는 기기에서는 말하지 않는다.** 한국어 목소리로 영어를
  읽히느니 자막만 보여 주는 편이 낫다.
- Chrome 은 `getVoices()` 를 처음 부를 때 빈 배열을 준다. 소리를 켜는 첫 제스처에
  미리 불러 두고, `voiceschanged` 가 오면 다시 고른다.
- 고른 목소리가 소리를 못 냈으면(서버 목소리인데 망이 막힌 경우) 그 목소리를
  걸러 내고 다음 대사부터 기기에 있는 목소리로 읽는다.

### 웅얼거리는 소리를 대사 위에 겹쳐 내고 있었다

`scream(… 'shout' | 'defy' | 'surrender')` 은 모음만 있는 합성 목소리다. 말 대신
내는 소리인데, 이것을 실제 대사와 **동시에** 내고 있었다. 대사가 제대로 발음될
때는 이 소리를 내지 않는다. 영어 목소리가 없거나, 너무 멀거나 벽 너머라 말이
들리지 않을 때만 낸다.

### 자막을 다시 넣었다 (아래쪽에 작게)

4차에서 자막을 걷어낸 이유는 **화면 한가운데를 가렸기** 때문이었다. 이번에는
조준선에서 멀리 떨어진 아래쪽에 작게 깐다(체력·탄약 줄 위). 영어 목소리가 없는
기기에서도 무슨 말인지는 읽을 수 있어야 한다.

동료가 외친 경고도 이제 같은 영어 대사로 들린다. 대사 문구를 그대로 서버에
보내면 남의 화면에 아무 글이나 띄울 수 있으므로, 목록의 **번호**만 오간다.

### '인질' 이름표가 인질범에게 붙어 있었다

인질은 붙잡힌 시민이다. 그런데 서버 안에서 `npc.hostage` 가 두 가지 뜻으로
쓰이고 있었다.

| | `npc.hostage` | 뜻 |
|---|---|---|
| 민간인 | `true` | 내가 붙잡혀 있다 (인질 본인) |
| 주요 용의자 | `'hostage'` | 내가 저 사람을 붙잡고 있다 (인질범) |

스냅샷은 이 둘을 `hostage: !!n.hostage` 한 필드로 합쳐 보냈다. 그래서 인질을
붙잡은 주요 용의자 머리 위에 '인질' 이 떴다. 뜻이 다른 값이니 따로 보낸다.

- `hostage` — 붙잡혀 있는 시민 (민간인에게만)
- `holdingHostage` — 시민을 방패로 삼은 자 (용의자에게만)

화면 쪽도 민간인일 때만 '인질' 을 붙이도록 막아 두었다. 옛 서버에 붙어도
인질범이 '인질' 로 보이지는 않는다.

## 2026-09-14 (4차) — 소리가 "작아서" 안 들리던 문제

플레이 중 보고: 아직도 소리가 안 난다 / 경고를 누르면 화면에 자막이 뜬다 /
안내 문구가 화면 한가운데를 가린다.

### 소리는 나고 있었다. 너무 작았을 뿐이다

헤드리스 브라우저에는 스피커가 없어서 실시간 분석기로는 아무것도 못 잰다.
`OfflineAudioContext` 로 같은 코드를 렌더링해 파형을 직접 재 보니 원인이 나왔다.

| 소리 | 고치기 전 (최대 진폭) | 고친 뒤 |
| --- | --- | --- |
| 총성(소총) | 0.25 | 0.72 |
| 문 열기 | 0.04 | 0.25 |
| 장전 | 0.08 | 0.57 |
| 수갑 | 0.06 | 0.52 |
| 비명 | **0.046** (-26dBFS) | 0.45 |
| 해체 작업 | 0.04 | 0.29 |

비명·수갑·장전은 -30 ~ -22dBFS 였다. 게임 배경음과 주변 소음에 묻혀 사실상
들리지 않는 크기다. 특히 목소리 계열은 포먼트 필터 두 겹을 지나며 원래 값의
1/13 까지 줄어 있었다.

- 공통 배율(`BOOST`)과 목소리 보정(`VOX_MAKEUP`)을 넣고, 문·장전·수갑·작업음의
  개별 값을 올렸다. (`public/js/audio.js`)
- 리미터(DynamicsCompressor)를 한 번 넣었다가 **뺐다.** 어떤 설정을 줘도 그
  노드를 지나는 것만으로 12dB 가까이 깎여 총성이 오히려 작아졌다(측정으로 확인).
  대신 소리마다 최대 크기를 1.0 아래로 맞췄다.
- 메뉴에 **소리 크기 슬라이더**와 **소리 테스트 버튼**을 넣었다. 브라우저가 막고
  있는 건지, 꺼 둔 건지, 기기 음량 문제인지 눌러 보면 바로 구분된다.
- iOS 처럼 `interrupted` 상태로 가는 경우에도 다시 깨우도록 고쳤다.
  ('suspended' 만 보고 있었다)
- 크기를 재는 도구를 남겼다: `npm run audio:levels`. 브라우저 검사
  (`scripts/browser-smoke.mjs`)에도 "총성 0.45 이상" 같은 최소치를 넣어, 다시
  작아지면 검사가 실패한다.

### 화면

- **구두 경고(V)는 이제 화면에 아무것도 띄우지 않는다.** 소리로만 나간다.
  항복·반항하는 상대의 대사도 자막 없이 소리로만 들린다. 체포해야 하는 상황만
  오른쪽 위 기록에 한 줄 남는다.
- **안내 문구(배너)를 화면 34% 에서 13% 로 올렸다.** 조준선과 문 앞 상황을
  가리고 있었다.
- 목표 패널의 위치 안내가 패널 밖으로 넘치던 것을 고쳤다. 세 곳 이상이면
  "… 외 N곳" 으로 줄인다.

## 2026-09-14 (3차) — 소리 · 경고 · 문 · 증거 · 배치

플레이 중 보고: 소리가 거의 없다 / 외곽 경비를 다 잡았는데 미션표가 그대로다 /
경고(V)를 눌러도 아무 일이 없다 / 해정·장비전환이 무슨 말인지 모르겠다 /
강제 개방과 열기의 차이를 모르겠다 / 문틈 확인이 글자만 뜬다 / 장부를 못 찾겠고
그 근처 그래픽이 깨진다 / 인질과 강도가 늘 같은 자리에 있다 / 섬광탄을 맞고도
적이 바로 쏜다 / 맞아도 비명이 없다.

### 소리

녹음 파일은 하나도 쓰지 않는다. 전부 브라우저 안에서 합성한다(외부 요청 없음).
사람 목소리는 톱니파(성대) + 대역통과 필터 두 개(입 모양)로 만든다.

- 새로 들어간 소리: **비명**(경상/중상/사망/공황/항복), **수갑**(래칫 7번 + 잠김),
  **해체·증거 회수·소생** 작업음(길게 누르는 동안 반복), **증거 집는 소리**,
  **안전핀**, **섬광 뒤 이명**, **가스 기침**, **해체 완료 확인음**,
  **다른 대원의 장전·고함**. (`public/js/audio.js`, `public/js/game.js`)
- **구두 경고는 실제로 말을 한다.** 브라우저 음성 합성으로 영어로 외치고
  ("Police! Drop the weapon!"), 뜻은 화면에 같이 띄운다. 항복·반항하는 상대도
  말로 대답한다. 음성 합성이 없는 브라우저에서는 고함 소리만 난다.
- **소리가 아예 안 나던 문제.** 브라우저는 사용자가 한 번 만지기 전에는 소리를
  막는다. 예전에는 `pointerdown`/`keydown` 두 가지만 듣고 있어서, 그 두 이벤트가
  오지 않는 경로(터치, 포인터 잠금 중의 클릭)에서는 영영 켜지지 않았다. 이제
  다섯 가지 제스처를 다 듣고, 멈춘 오디오 컨텍스트를 다음 소리마다 다시 깨우며,
  작전 시작 후 1.5초까지 소리가 안 켜졌으면 화면에 안내를 띄운다.

### 미션표

- 목표 상태가 바뀔 때마다 서버가 `objectives` 를 보낸다. 예전에는 **단계가 통째로
  끝날 때만** 보내서, 외곽 경비를 다 잡아도 "0/2" 가 그대로 남아 있었다.
  (`server.js`, `public/js/game.js`)
- 증거·장치 목표에는 **무엇이 어느 방에 남았는지**를 함께 적는다.
  ("증거 회수 1/3 — 거래 장부 · 서재 / 무전 기록 · 연회실")

### 구두 경고 (V)

- 외치면 주변 사람의 **압박(pressure)** 이 쌓이고, 압박은 사기를 깎는다. 시간이
  지나면 풀린다. 한 번 깎고 마는 값이 아니라 상태가 됐다.
- 결과는 확률로 갈린다. **총구를 겨눈 채**(조준선 ±26°) 가까이서, 상대가 다쳤고,
  동료가 쓰러졌고, 여러 번 외쳤을수록 항복 확률이 오른다. 실패하면 일부는
  **오히려 덤벼든다**(`npcDefy`). 인질을 잡은 자에게는 통하지 않는다.
- 외친 본인에게 결과를 돌려준다. 아무도 못 들었으면 "보이는 거리에서", 겨누지
  않았으면 "조준한 채로" 라고 알려 준다. (`public/js/suspect-ai.js`, `server.js`)

### 문

- 안내 문구를 동작 기준으로 다시 썼다. 괄호 안의 "시끄럽다" 가 문의 상태처럼
  읽히던 문제를 없앴다.
  - `E 열기 (조용함)` / `E 잠금 해제 · 약 4초 (조용함)` — "해정" 이라는 말을 버렸다
  - `B 강제 개방 — 발로 찬다 (큰 소리, 다 듣는다)`
  - `Q 문틈으로 보기(조용)`
  - 메뉴 화면에 네 동작의 차이를 따로 적어 뒀다.
- **문틈으로 보기가 실제 시점이 됐다.** 카메라가 문짝 바로 앞 열쇠구멍 높이로
  가고, 화면은 구멍 모양으로 가려지고, 시야각이 34°로 좁아진다. 문짝은 눈앞을
  가리지 않도록 잠깐 감춘다. 보는 동안에는 움직일 수도 쏠 수도 없다.
  (`public/js/game.js`, `public/js/world.js`, `public/js/hud.js`, `public/index.html`)
- 터치 버튼 "장비전환" -> "장비 바꾸기".

### 증거 (장부 · 드라이브 · 무전 기록 …)

- 바닥에 놓인 초록 상자 하나였다. 이제 물건마다 생김새가 다르고(장부는 책 더미,
  무전 기록은 안테나 달린 무전기), **빛기둥**이 서 있어 방 안에서 눈에 띄고,
  가까이 가면 **이름표**("거래 장부 / F 길게 눌러 회수")가 보인다.
- **서재 책장이 깨져 보이던 원인.** 책장 뒷면이 외벽면과 정확히 같은 평면에
  있었다(z-fighting). 벽에서 6cm 띄웠다. 같은 일이 다시 생기지 않도록
  "가구와 벽이 같은 평면에 놓이면 실패" 하는 검사를 넣었다.
  (`public/js/map-data.js`, `tests/mansion.test.mjs`)
- 증거는 방마다 조금씩 다른 자리에 놓이고, 벽·가구에 끼지 않게 보정한다.

### 배치

- **인질과 주범**: 농성하는 방(6곳), 그 방 안의 자리, 바라보는 방향을 매 판 새로
  뽑는다. 인질 자리 후보를 방마다 2~3곳으로 늘렸다.
- **무장 인원**: 담당 자리에서 ±1.3m 안으로 흩어 놓고 방향도 흔든다.
- 민간인은 한 방에 한 명씩, 주범의 방은 비워 둔다.

### 섬광탄과 비명

- 섬광에 걸리면 **최소 3초**는 아무것도 못 한다(사람도 용의자도 같은 규칙).
  세기에만 비례하던 예전 값으로는 스치듯 맞으면 1초도 안 돼 다시 쐈다.
  (`public/js/grenades.js` 의 `flashSeconds`)
- 맞으면 비명을 지른다. 피해가 클수록 크게 지르고, 쓰러질 때는 다른 소리를 낸다.
  연사에 맞아도 소리가 겹치지 않게 간격을 둔다.

### 테스트

99개 -> **111개**. 새로 확인하는 것:

| 파일 | 내용 |
| --- | --- |
| `tests/suspect-ai.test.mjs` | 경고 확률(겨눔/거리/부상/동료), 항복·반항·흔들림·쿨다운, 압박이 사기를 깎고 풀리는 것 |
| `tests/grenades.test.mjs` | 섬광 노출 시간의 최소 3초 보장 |
| `tests/mission.test.mjs` | 경비를 하나씩 처리할 때 보고서가 매번 달라지는 것, 증거·장치의 위치 안내 |
| `tests/audio.test.mjs` | 비명·수갑·작업·이명 등 새 소리, 남의 장전이 내 장전을 끊지 않는 것, 음성 합성이 없는 환경, 멈춘 오디오 재개 |
| `tests/assets.test.mjs` | 역할별 모델이 없을 때 기본 캐릭터로 내려가는 것 |
| `tests/mansion.test.mjs` | 가구가 벽과 같은 평면에 놓이지 않는 것 (z-fighting) |
| `tests/server.test.mjs` | 목표가 바뀌면 `objectives` 가 즉시 오는 것, 경고 결과 회신 |
| `scripts/browser-smoke.mjs` | 열쇠구멍 시점(문짝 감춤 / 이동 잠금 / 시야각) |

### 캐릭터 모델

- 역할별 모델을 넣을 수 있다. `character-officer.glb`(대원) /
  `character-suspect.glb`(납치범) / `character-hostage.glb`(인질·민간인).
  **넣은 것만 적용**되고 나머지는 지금 모델을 그대로 쓴다. 동작 16개는 공유한다.
  `node scripts/swap-character.mjs --role hostage <파일>`
- 받을 수 있는 곳과 절차는 `CHARACTER-MODELS.md` 에 정리했다.

## 2026-09-14 (2차) — 플레이 중 보고된 버그

플레이 중 보고: 적이 안 보이는데 총을 맞는다 / 저택 안에서 시작한다 / 움직이면
천장이 깨진다 / 문 앞 상호작용이 안 먹는다 / 남의 캐릭터가 총을 이상하게 들고
있다 / 적 외형이 어색하다 / 소리가 부족하다.

### 화면에서 고친 것

- **총을 이상하게 들고 있던 문제.** 예전에는 "오른손 → 왼손" 방향 하나만 총구에
  맞췄다. 축 하나만 맞추면 총이 총열을 축으로 제멋대로 굴러 옆으로 눕거나
  뒤집힌다. 게다가 달리기·측면이동·앉기·사망 클립에서는 두 손이 총을 잡은 모양이
  아니어서 총구가 엉뚱한 데를 가리켰다. 총구 축과 총 윗면 축을 같이 세워 회전을
  완전히 고정했고, 두 손이 총을 잡은 모양일 때만 손 방향을 쓴다. 아닐 때는 몸을
  기준으로 총을 내린 자세로 붙인다. 뼈가 없는 대체 모델도 가슴 높이로 든다.
  (`public/js/character-animation.js`)

- **움직일 때 천장이 깨지던 문제.** 벽과 상인방 40개의 윗면이 천장면과 정확히
  같은 높이(y = 7m)에 있었다. 깊이 값이 같으면 카메라가 움직일 때마다 어느 쪽이
  앞인지 뒤집히며 깜빡인다(z-fighting). 천장에 닿는 벽은 8cm 더 올려 그려서 겹치는
  면 자체를 없앴다. 지붕도 위로 올렸고, 지면/실내 바닥/진입로의 간격도 벌렸다.
  브라우저 검사에 "천장과 같은 높이로 끝나는 면 0개" 조건을 넣었다.
  (`public/js/world.js`, `scripts/browser-smoke.mjs`)

- **적을 못 보고 맞던 문제.** 야간 작전이라 사수를 보기 전에 맞는 일이 잦다.
  - 용의자가 나를 발견하는 순간 그 대원에게만 `spotted` 를 보낸다. 화면 가장자리에
    방향 쐐기가 뜨고, 발견을 외치는 소리가 그 방향에서 들린다. 총알보다 먼저 온다.
  - 피격에도 사수 위치를 실어 보내 같은 방향 표시를 띄운다.
  - 대원/용의자/주요 용의자/민간인에게 방탄조끼·어깨 표식·헬멧을 달았다. 어깨
    표식은 스스로 빛나서 광원 없는 복도에서도 구분된다.
  (`server.js`, `public/js/game.js`, `public/js/hud.js`, `public/js/entities.js`,
   `public/index.html`)

- **문 상호작용이 안 먹던 문제.**
  - 터치 버튼이 문 근처이기만 하면 세 개 다 나타났다. 바리케이드 문 앞에서 "문" 을
    눌러도 아무 일도 일어나지 않았다. 이제 지금 할 수 있는 동작만 띄우고, 버튼
    글자도 열기/닫기/해정으로 바뀐다.
  - 손이 닿는 거리를 문 중심점이 아니라 **문틀 선분**까지의 거리로 잰다. 폭 2.4m 인
    정문은 문 앞에 바짝 서 있어도 중심이 멀어 안내가 사라지곤 했다. 화면과 서버가
    같은 함수(`doorDistance`)를 써서, 안내가 떴는데 눌리지 않는 상황을 없앴다.
  - 부서진 문에는 남은 동작이 없으므로 안내를 띄우지 않는다.
  (`public/js/doors.js`, `public/js/hud.js`, `public/js/game.js`, `server.js`)

- **장전 소리가 시작하자마자 끊기던 문제.** 장전을 요청하면 화면은 바로 "장전 중"
  으로 바꾸는데, 그 직후 도착하는 스냅샷은 아직 요청이 반영되기 전 상태다. 그걸
  "장전이 취소됐다" 로 읽고 소리를 즉시 멈추고 있었다. 서버가 한 번이라도 장전
  중이라고 알려 준 뒤에만 취소로 본다. (`public/js/game.js`)

### 소리

원래 있던 총성·장전음·문 소리에 더해 직접 합성으로 추가했다. 내려받은 녹음이나
외부 요청은 없다.

- 총성에 실내 잔향 꼬리와 탄피 소리를 얹었다. 같은 총도 방 안과 밖이 다르게 들린다.
- 장전을 네 박자(탄창 멈치 → 빈 탄창 낙하 → 새 탄창 삽입 → 노리쇠 전진)로 나눴다.
- 문 소리를 동작별로 나눴다. 강제 개방은 방 전체가 알아챌 만큼 크고, 해정은 걸쇠를
  네 번 건드리고, 문틈 확인은 거의 들리지 않는다.
- 빈 약실(딸깍), 탄착, 피격, 발각 외침, 폭발/섬광/가스, 발소리를 추가했다.
  발소리는 걸은 거리로 박자를 잡아서 속도가 바뀌어도 보폭이 일정하다. 앉아서
  움직이면 거의 들리지 않는다.
  (`public/js/audio.js`, `public/js/game.js`)

### 정리

- 단계 목표 판정을 `public/js/objectives.js` 로 빼냈다. 서버 틱 안에 묻혀 있어서
  단계 전환을 확인할 방법이 판을 끝까지 돌리는 것밖에 없었다.
- 브라우저가 자동으로 찾는 `/favicon.ico` 404 를 없앴다(문서 안에 SVG 아이콘).

### 캐릭터 모델

지금 모델은 Mixamo 의 판타지 전사(Dreyar)라 작전과 어울리지 않는다. 모델 파일
하나만 주면 동작 16개를 그대로 옮겨 붙이는 `scripts/swap-character.mjs` 를 만들었다.
받을 곳과 절차는 `CHARACTER-MODELS.md` 에 정리했다.

### 테스트

69개 → **99개**. 새로 추가한 것:

| 파일 | 내용 |
| --- | --- |
| `tests/character-rig.test.mjs` | 총을 든 자세 4가지 (손 방향 / 내린 총 / 뼈 없는 모델 / 실제 GLB + 장구류 크기) |
| `tests/mission.test.mjs` | 단계 데이터 정합성, 목표 9종 판정, 단계 전환, 등급 계산 20개 |
| `tests/doors.test.mjs` | 넓은 문의 손닿는 거리, 부서진 문, 안내한 동작은 전부 결과가 있는지 |
| `tests/audio.test.mjs` | 총성 겹 수(내 총 6 / 남의 총 4), 문 동작별 소리, 피격·발각 방향 |

브라우저 검사(`scripts/browser-smoke.mjs`)가 **처음으로 끝까지 통과**했다. 새로
검사하는 것: 천장과 같은 높이로 끝나는 면 0개, NPC 의 총이 손에 붙어 있고 바로
서 있는지, 장구류가 몸에 맞는 크기인지.

---

## 2026-09-14 (1차)

- 첫 화면의 셰이더 컴파일을 로비 로딩 단계에서 완료해 서버 전투가 시작된 뒤 입력이 멈추는 시간을 줄였다.
- 천장 높이 7m와 지붕 밑면 7m가 겹치던 면을 5cm 분리했다. 야외 가로등에 실내용 샹들리에를 생성하던 장식도 제외했다.
- 서버/화면의 호환 버전을 방 참가 전에 검사한다. 구형 서버의 실내 시작 좌표, `bots` 메시지, 문 이벤트 부재가 새 화면과 섞여 보이지 않는 적의 공격으로 이어지는 것을 막는다. 시작 이벤트도 별도로 검사한다.
- 문 요청 직전에 현재 위치를 순서가 보장되는 입력으로 전송한다. 응답 지연·거리·작업 중·상태 변경을 안내하며 다음 스냅샷에서 문 상태를 다시 동기화한다.
- 다음 스냅샷에서 누락된 NPC를 생성한다. 미확정 사격 예측만으로 살아 있는 NPC의 사망 동작을 재생하지 않는다.
- 캐릭터의 원래 피부·의상 텍스처를 보존한다. 전체를 붉게 칠하던 처리를 제거했다. 이름표는 벽 뒤에서 보이지 않게 한다.
- 소총·기관단총·저격총 발사음, 탄창 탈착·삽입·장전음, 문 동작음을 직접 합성한다. 원격 사격에는 좌우 방향, 거리 감쇠, 벽 차폐를 적용한다. 효과음 설정을 메뉴에 추가했다.
- 소리는 사용자 입력 이후 활성화하며, 매치 종료·나가기·음소거 시 재생과 예약된 장전음을 중지한다.

---

## 배포에서 반드시 맞출 것

프론트엔드: `https://game33-eta.vercel.app`
게임 서버: `https://market-raid.onrender.com`

**화면과 게임 서버가 같은 브랜치여야 한다.** 서로 다르면 `/health` 의 `protocol`
값이 어긋나고, 화면은 "게임 서버 업데이트가 아직 완료되지 않았습니다" 를 띄운다.

### 이번에 실제로 어긋나 있던 것

배포 기록을 확인한 결과는 이렇다.

| | 무엇이 올라가 있었나 |
| --- | --- |
| Vercel 운영 (`game33-eta.vercel.app`) | 커밋 `208c7ea` (전술 진입 초기판)을 수동 승격 |
| Render 게임 서버 | 옛 `main` (시장 맵 시절) |

**시작 위치, 적 목록, 문 이벤트는 전부 서버가 정한다.** 화면만 새 버전이고
서버가 옛 `main` 이면 이렇게 된다.

- 옛 `main` 의 시작 좌표는 `z = 15`, 즉 저택 **안**이다 → "밖에서 시작한다더니 안에서 시작"
- 옛 `main` 은 NPC 를 `bots` 로 보낸다. 새 화면은 `npcs` 를 기다리므로 아무도 안 그려진다.
  그런데 서버의 봇은 계속 쏜다 → "안 보이는 적이 공격한다"
- 옛 `main` 에는 `door` 처리 자체가 없다 → "문 앞 상호작용이 하나도 안 먹는다"

천장 깜빡임과 총 자세는 화면 쪽 버그였고 이번에 고쳤다.

### 배포 순서

1. 이 수정을 `main` 에 합친다.
2. Vercel 은 `main` 을 운영 배포로 자동으로 따라간다.
3. **Render 대시보드에서 게임 서버를 `main` 으로 배포한다.** 저장소의
   `render.yaml` 변경만으로는 이미 만들어진 서비스의 브랜치가 바뀌지 않는다.
   Settings → Build & Deploy → Branch 를 `main` 으로 맞추고 Manual Deploy 를
   누른다.
4. `https://market-raid.onrender.com/health` 를 열어 `protocol` 이
   `ravenwood-entry-1` 인지 확인한다. 이 값이 다르면 아직 옛 서버다.

## 검증

```sh
npm ci
npm test
node scripts/asset-integrity.mjs --write
npm run build:static
npm install --no-save --package-lock=false playwright@1.58.2
npx playwright install chromium
node scripts/browser-smoke.mjs
```

미리 받아 둔 Chromium 을 쓰려면 `CHROMIUM_PATH=/opt/pw-browsers/chromium` 을 앞에
붙인다.

브라우저 검증이 확인하는 것: 야외 시작, 실제 GLB 애니메이션의 위치/크기, 천장
깜빡임 원인 제거, 총을 든 자세와 장구류, Q 문틈 확인, E 열기/해정 또는 B 강제
개방, 서버 상태와 충돌 동기화, 사격·장전 오디오, 구형 매치 거부, 콘솔 오류 0.
프레임 전체의 시각적 품질이나 모든 기기의 GPU를 보증하지는 않는다.

모델/텍스처 원본은 변경하지 않아 에셋 해시는 같아야 한다. CI에서 해시 재생성 후
차이가 없는지도 검사한다.
